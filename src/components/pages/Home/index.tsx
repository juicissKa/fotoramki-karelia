import React from "react";
import CategoryCard from "./CategoryCard";
import styles from "./Home.module.scss";

const Home: React.FC = () => {
  return <div className={`container ${styles.root}`}></div>;
};

export default Home;
